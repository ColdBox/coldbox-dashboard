<cfscript>               function listDrivesAndRootFolders(){            // list root drives            roots = createObject("java"," java.io.File").listRoots();            for(i=1;i lte ArrayLen(roots);i=i+1){                WriteOutPut(roots[i].getAbsolutePath());                fileList =     roots[i].listFiles();                               if(isDefined("fileList")){                    WriteOutput("<blockquote>");                    for(j=1;j lte ArrayLen(fileList);j = j + 1){                        if(fileList[j].isDirectory()){                            WriteOutput("   " & fileList[j].getName() & "</br>");                            //listDrivesAndFolders(fileList[j]);                        }                    }                    WriteOutput("</blockquote>");                }            }                   }               listDrivesAndRootFolders();           </cfscript>